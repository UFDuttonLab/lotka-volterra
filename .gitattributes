# Normalise line endings so Windows checkouts do not rewrite every file.
# After adding this, run once: git add --renormalize . && git commit
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.svg text eol=lf
*.lockb binary
